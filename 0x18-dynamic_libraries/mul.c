#include <stdio.h>
/**
 * mul - multiply two integers and returns the result
 * @a: first number
 * @b: second number
 * Return: result of multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
