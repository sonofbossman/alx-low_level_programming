#include <stdio.h>
/**
 * main - entry of the program
 * printf - print out digits
 * Return: 0 if successful
 */
int main(void)
{
	int num = 0;
while (num <= 9)
{
printf("%d", num);
num++;
}
printf("\n");
return (0);
}
